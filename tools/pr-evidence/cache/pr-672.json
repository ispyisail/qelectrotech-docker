{"body":"Fixes #671.\n\n`autonum::elementPrefixForLocation()` resolves an element's IEC 81346 designation prefix from `qet_labels.xml` (surfaced as the `%prefix` label-formula variable). The analysis in #671 found six defects in it; this rewrites the function rather than patching around them, because several dissolve entirely once the parsing is done properly. Net: 115 lines → ~50, plus a ~45-line helper.\n\n## What changes\n\n1. **Relocated common collections work again.** `commonElementsDir()` returns the configured path verbatim with no trailing separator, and the old code did `commonElementsDir().append(\"10_electric/qet_labels.xml\")` — producing a mangled path and a silent empty prefix. Now joined with `QDir::filePath()`.\n2. **Each common-collection top-level tree gets its own candidate**, so `20_logic`, `30_hydraulic` etc. can carry a `qet_labels.xml` instead of being sent to the user's collection by the hardcoded `!= \"10_electric\"` test.\n3. **`QString path[10]` with an unbounded `i++` replaced by a `QStringList`.**\n4. **The company collection is now considered** alongside common and custom.\n5. **Nesting-aware parsing.** The old flat token scan matched any element whose `name` attribute equalled the next path segment with no parent/child check, and implemented \"inherit the parent's prefix\" by scanning *forward* — which only works because the file format requires `<prefix>` after child `<category>` elements. Replaced with a `QDomDocument` walk that descends by actual nesting and remembers the nearest ancestor prefix, so ordering within a category no longer matters. Existing files keep working.\n6. Removed two no-op `file.isReadable();` statements and ~40 lines of copy-paste between the two collection branches.\n\n## Behaviour change worth flagging\n\nAn element in a directory the labels file doesn't mention now inherits its **nearest listed ancestor's** prefix instead of getting none. That's what the format documents (\"If a directory does not have a prefix the element will assign its parent directory prefix and so on\") — the old forward-scan couldn't implement it. Practically it means more elements get a sensible prefix than before, but it is a change.\n\n## On collection identity\n\nWhich collection an element came from isn't recoverable post-import (`addElement()` builds the embedded path from `collectionPath(false)`, which strips the protocol), so candidates are tried in turn and the first that describes the element's path wins. Custom files written against the common-collection layout are still accepted, so existing setups don't break.\n\nIf you'd rather fix this at the root by having `commonElementsDir()` guarantee a trailing separator, that's a smaller change — only one caller in the tree concatenates onto it directly, and it's the buggy line. I went with the local fix to keep the blast radius small; happy to switch.\n\n## Test plan\n\nVerified against the real collection with A/B runs of the same binary before and after, reading the `prefix` attribute out of the saved `.qet`:\n\n| case | before | after |\n|---|---|---|\n| relocated common collection (no trailing `/`) | `prefix=\"\"` | `prefix=\"K\"` |\n| default install | `prefix=\"K\"` | `prefix=\"K\"` (no regression) |\n| second common tree with its own `qet_labels.xml` | `prefix=\"\"` | `prefix=\"Z\"` |\n| inheritance: element under `90_terminal_strips_diagram` (no prefix of its own) | `prefix=\"X\"` | `prefix=\"X\"` |\n\nThe coil used is from `10_allpole/310_relays_contactors_contacts/01_coils`, which declares `<prefix>K</prefix>`.\n\nNot reproduced, fixed by construction: the `path[10]` overflow (would need a custom collection nested >9 deep) — see #671 item 3.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n","commits":[{"authoredDate":"2026-08-06T09:45:35Z","authors":[{"email":"shane.ringrose@gmail.com","id":"MDQ6VXNlcjEyMTE2MTEw","login":"ispyisail","name":"ispyisail"}],"committedDate":"2026-08-06T09:45:35Z","messageBody":"…es, bounds\n\nautonum::elementPrefixForLocation() reads qet_labels.xml to give each\nelement its IEC 81346 designation prefix (surfaced as the %prefix\nlabel-formula variable). It had several defects; this rewrites it.\n\n1. A relocated common collection silently lost every prefix.\n   QETApp::commonElementsDir() returns the user's configured path\n   verbatim, without appending a trailing \"/\" (unlike its sibling\n   customElementsDir(), which normalises). The lookup then did naive\n   string concatenation:\n\n       QETApp::commonElementsDir().append(\"10_electric/qet_labels.xml\")\n\n   producing \"<path>10_electric/qet_labels.xml\" -- a mangled path, file\n   not found, no prefix, no error anywhere. Default installs were\n   unaffected because both build systems bake in a path that happens to\n   end in \"/\", which is why this never showed up in development. This is\n   very likely what users hit when reporting that prefixes stop working\n   once the collection is moved out of its default location. Now joined\n   with QDir::filePath(), which is separator-correct either way.\n\n2. Common-collection trees other than 10_electric read the wrong file.\n   The branch condition was literally\n   `if (current_location.fileName() != \"10_electric\")`, sending\n   everything else down the custom-collection path. The common collection\n   ships five top-level trees (10_electric, 20_logic, 30_hydraulic,\n   50_pneumatic, 60_energy), so an element from any of the other four\n   looked for its labels in the *user's* collection. Each top-level tree\n   now gets its own candidate, so dropping a qet_labels.xml into any of\n   them works.\n\n3. Fixed-size array with an unbounded index. `QString path[10]` was\n   filled by a `while` loop doing `i++; path[i] = ...` with no bound.\n   The deepest category in the shipped collection needs 9 of those 10\n   slots; a custom collection can nest arbitrarily deep, and overflowing\n   writes QString objects past the end of a stack array. Replaced with a\n   QStringList.\n\n4. The company collection was never considered at all. It is now a\n   candidate alongside common and custom.\n\n5. The XML matching ignored nesting. It was a flat token scan matching\n   any element whose \"name\" attribute equalled the next path segment,\n   with no check that the match was a child of the previous one; it\n   worked on the shipped file only because depth-first document order\n   happens to line up. Inheritance (\"a category with no prefix uses its\n   parent's\") was implemented by scanning *forward*, which can only work\n   because the file format requires <prefix> to come after child\n   <category> elements -- a constraint that exists solely to accommodate\n   that parser. Replaced with a QDomDocument walk that descends by actual\n   nesting and remembers the nearest ancestor prefix, so ordering within\n   a category no longer matters. Existing files keep working.\n\nAlso removes two no-op `file.isReadable();` statements (return value\ndiscarded) and ~40 lines of copy-pasted duplication between the two\ncollection branches.\n\nNote one intentional behaviour change: an element in a directory the\nlabels file does not mention now inherits its nearest listed ancestor's\nprefix instead of getting none. That is what the format documents (\"If a\ndirectory does not have a prefix the element will assign its parent\ndirectory prefix and so on\"); the old forward-scan could not implement it.\n\nWhich collection an element came from is not recoverable after import --\nXmlElementCollection::addElement() builds the embedded path from\ncollectionPath(false), which strips the common:// / custom:// /\ncompany:// protocol -- so the candidates are tried in turn and the first\nthat describes the element's path wins. Custom files written against the\ncommon-collection layout are still accepted, so existing setups do not\nbreak.\n\nVerified against the real collection, with A/B runs of the same binary\nbefore and after, reading the saved .qet's prefix attribute:\n- relocated common collection: prefix=\"\" before, prefix=\"K\" after\n- default install: prefix=\"K\" before and after (no regression)\n- second common tree with its own qet_labels.xml: prefix=\"\" before,\n  prefix=\"Z\" after\n- inheritance: an element under 90_terminal_strips_diagram, which has no\n  prefix of its own, still resolves to \"X\" from its parent category","messageHeadline":"Fix element prefix lookup: relocated collections, non-10_electric tre…","oid":"a147841206ded1e98be9c54e809f1b6b5136e7a5"}],"number":672,"state":"OPEN","title":"Fix element prefix lookup: relocated collections, non-10_electric trees, unbounded array","url":"https://github.com/qelectrotech/qelectrotech-source-mirror/pull/672"}
