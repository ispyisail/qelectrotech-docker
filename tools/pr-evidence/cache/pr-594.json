{"body":"Closes discussion #579.\n\n## Problem\n\nThere's no way to auto-number elements/conductors alphabetically (`a`, `b`, ... `z`, `aa`, `ab`, ...) — only numerically via `unit`/`ten`/`hundred`. `StringNum`, the closest existing part type, is a fixed/constant text segment (`nextString()` just re-adds the current value unchanged); there's no letter-incrementing anywhere in the codebase.\n\n## Change\n\nA new `AlphaNum` leaf strategy (`sources/autoNum/numerotationcontextcommands.{h,cpp}`), the 15th `NumStrategy` subclass:\n\n- `incrementAlpha()`/`decrementAlpha()` implement the spreadsheet-column-name algorithm: increment carries right-to-left on `z`/`Z` overflow, prepending a new leading letter if the whole value overflows (`z → aa`, `az → ba`). Decrement is the exact inverse, including the symmetric shrink case (`aa → z`) once every position has borrowed.\n- As the discussion correctly predicted, this is genuinely smaller than #578 (wrap-and-carry) — the carrying happens entirely within one part's own value, so `NumerotationContextCommands`'s composition loop needed zero changes.\n- Registered in `NumerotationContext::validRegExpNum()` but deliberately **not** in `validRegExpNumber()`, so `addValue()` doesn't force alphabetic values through int conversion — exactly as scoped in the discussion.\n- New \"Alphabétique\" entry in `numparteditorw`'s type dropdown, with its own letters-only `QRegularExpressionValidator`. The increase spinbox is disabled since the step is always exactly one letter, not a configurable amount (its minimum was already `1`, which doesn't apply here since there's no numeric step to configure).\n\n## One thing the discussion didn't cover: this needed a second layer to actually reach labels\n\nFolio numbering applies a `NumerotationContext`'s represented string directly, so `AlphaNum` works there with zero extra plumbing. But **element and conductor numbering** — the discussion's explicit primary use case — go through a separate formula-substitution layer: `autonum::sequentialNumbers` + `%sequ_N`/`%seqt_N`/`%seqh_N`-style placeholders resolved in `AssignVariables::assignSequence()`, auto-populated by `numerotationContextToFormula()`. Without a matching placeholder, an `alpha` part would silently disappear from the generated formula and never reach the actual label — the counter would advance correctly in the background while the UI looked complete, but element/conductor labels would just never show a letter.\n\nSo this PR also:\n- Adds an `alpha` `QStringList` member to `sequentialNumbers` (copy ctor, `operator=`, `operator==`, `toXml`/`fromXml`, `clear()`).\n- Adds a `%seqa_N` placeholder, emitted by `numerotationContextToFormula()` for `alpha` parts the same way `%sequ_N` is emitted for `unit`.\n- `setSequential()`/`setSequentialToList()` populate `seqStruct.alpha`, passing the raw string through as-is (not the `.toInt()`-based formatting the numeric types use).\n- `AssignVariables::assignSequence()` substitutes `%seqa_N`, mirroring the existing numeric substitutions.\n\nNo `alphafolio` variant was added, matching scope — only `unit`/`ten`/`hundred` have folio-anchored variants today.\n\n## Testing\n\nBuilt clean (no new warnings). Verified against production code via the numbering config dialog's own Suivant/Précédent buttons:\n- `a` + 25 clicks → `z`; one more → `aa`; 25 more → `az`; one more → `ba` (carry).\n- Reversed: `ba → az` → (25 clicks) → `aa` → `z` (shrink) → (25 clicks) → `a`.\n- One more \"previous\" at `a` correctly **stays** at `a` (see below).\n- The Formule field auto-updates to `%seqa_1` the instant the type is switched to \"Alphabétique\", confirming the formula-generation wiring works live, not just at the engine level.\n\n**Bug caught and fixed during this testing**: the initial `decrementAlpha()` mutated the string in the borrow loop *before* checking whether to clamp a single letter at its minimum, so `a` → previous incorrectly produced `z` instead of staying at `a`. Fixed by handling the single-letter case up front, before any mutation.","commits":[{"authoredDate":"2026-08-01T09:34:20Z","authors":[{"email":"shane.ringrose@gmail.com","id":"MDQ6VXNlcjEyMTE2MTEw","login":"ispyisail","name":"ispyisail"}],"committedDate":"2026-08-01T10:45:49Z","messageBody":"Adds a real base-26 incrementing part type to the autonumbering engine,\nalongside the 14 existing NumStrategy leaves. Unlike StringNum (a fixed,\nnon-incrementing text segment), AlphaNum::next()/previous() carry/borrow\nentirely within the part's own value -- the composition loop in\nNumerotationContextCommands doesn't need to change, since (unlike #578's\nwrap-and-carry) nothing here needs to signal an adjacent part.\n\n- incrementAlpha()/decrementAlpha() implement the spreadsheet-column-name\n  algorithm: increment carries right-to-left on 'z'/'Z' overflow,\n  prepending a new leading letter if the whole value overflows (z -> aa,\n  az -> ba). decrement is the exact inverse, including the symmetric\n  shrink case (aa -> z) once every position has borrowed. A single letter\n  already at \"a\"/\"A\" has no representable predecessor and is clamped\n  rather than turned into \"z\" -- caught via manual testing, since the\n  initial implementation mutated the string in the borrow loop before\n  checking whether to clamp, silently discarding the original value.\n- Registered in NumerotationContext::validRegExpNum() but deliberately\n  not in validRegExpNumber(), so addValue() doesn't force alphabetic\n  values through int conversion.\n- New \"Cyclique\"-adjacent \"Alphabétique\" entry in numparteditorw's type\n  dropdown, with its own letters-only QRegularExpressionValidator; the\n  increase spinbox is disabled since the step is always exactly one\n  letter, not a configurable amount.\n\nAlso wires the new part type through to actual element/conductor labels,\nwhich turned out to be required for the feature to do anything visible\nbeyond folio numbering (which applies a NumerotationContext's\nrepresented string directly). Element and conductor numbering instead\ngo through a separate formula-substitution layer\n(autonum::sequentialNumbers + %sequ_/%seqt_/%seqh_-style placeholders in\nAssignVariables::assignSequence()) that numerotationContextToFormula()\nauto-populates. Without a matching placeholder, an \"alpha\" part would\nsilently vanish from the generated formula and never reach the label,\neven though the underlying counter was advancing correctly:\n- sequentialNumbers gained an `alpha` QStringList member (copy ctor,\n  operator=, operator==, toXml/fromXml, clear()).\n- numerotationContextToFormula() emits a new %seqa_N placeholder for\n  alpha parts, the same way %sequ_N is emitted for unit parts.\n- setSequential()/setSequentialToList() populate seqStruct.alpha,\n  passing the raw string through as-is rather than the .toInt()-based\n  formatting used for the numeric part types.\n- AssignVariables::assignSequence() substitutes %seqa_N from\n  seqStruct.alpha, mirroring the existing %sequ_N/%seqt_N/%seqh_N\n  substitutions.\nNo \"alphafolio\" variant was added, matching the discussion's scope (only\nunit/ten/hundred have folio-anchored variants).\n\nVerified against production code via the numbering config dialog's own\nSuivant/Précédent buttons: from \"a\", 25 clicks reached \"z\"; one more\nproduced \"aa\"; 25 more reached \"az\"; one more produced \"ba\" (carry).\nReversed: \"ba\"->\"az\"->(25 clicks)->\"aa\"->\"z\" (shrink)->(25 clicks)->\"a\".\nOne more \"previous\" at \"a\" correctly stayed at \"a\" after the clamp fix.\nAlso confirmed the Formule field auto-updates to \"%seqa_1\" the instant\nthe type is switched to \"Alphabétique\", confirming the formula-generation\nwiring works live in the UI, not just at the engine level.","messageHeadline":"Add alphabetical auto-numbering (a, b, ... z, aa, ab, ...) (#579)","oid":"f7a79e75af338b477d1049e9f095ec48a8721b76"}],"number":594,"state":"MERGED","title":"Add alphabetical auto-numbering (a, b, ... z, aa, ab, ...)","url":"https://github.com/qelectrotech/qelectrotech-source-mirror/pull/594"}
