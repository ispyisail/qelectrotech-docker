{"body":"Builds discussion #607.\n\n## Problem\n\nCutting/copying a linked group of elements (a relay coil with its contacts, a PLC master with its slave I/O elements, etc.) — even the whole group together in one paste — currently drops the master/slave link entirely. The pasted copies come in as ordinary, independent elements with no link and no warning; the user has to notice and manually re-link them afterward.\n\n## Traced end to end\n\n- `Element::toXml()` writes each linked partner's uuid into a `<link_uuid>` entry — the clipboard XML genuinely contains the link information.\n- `Element::fromXml()` reads that back — but only into a **deferred, unresolved** buffer, `tmp_uuids_link`. No linking happens at this point.\n- The only code that ever resolves `tmp_uuids_link` into a real link is `Element::initLink(QETProject *prj)`, which builds an `ElementProvider` and resolves the pending uuids project-wide.\n- `initLink()` is only ever called from `Diagram::refreshContents()`, which in turn is only called from two places: full project load, and macro-block insertion.\n- `DiagramView::paste()` calls `Diagram::fromXml()` directly and never calls `refreshContents()` or `initLink()` anywhere in that path. Pasted elements' `tmp_uuids_link` gets populated correctly, but nothing ever resolves it — the link is silently dropped.\n\nThis is a different, narrower problem from the uuid-*collision* issue already fixed for whole-folio duplication (an element keeping its source uuid as the project database's primary key) — that's a separate bug on a separate code path.\n\n## Fix\n\nAdded `Element::initLink(const QList<Element *> &candidates)` — resolves pending links against a caller-supplied candidate list instead of a project-wide `ElementProvider` search.\n\nThe scoping matters and is the subtle part: right after an XML round-trip and *before* `PasteDiagramCommand::redo()`'s existing `newUuid()` loop renews each pasted element's identity, a pasted element's `tmp_uuids_link` still holds its source's original partner uuid — which at that exact moment still equals the not-yet-renewed uuid of that partner's own pasted copy, if it was pasted too. Resolving only within the pasted batch (not the whole project) is what stops a linked pair being pasted together from matching against an original element left elsewhere in the project that happens to still carry that same soon-to-be-replaced uuid. If only one half of a linked group is pasted, its entry simply finds no match in the batch and is dropped — the same \"leave it unlinked\" outcome as today.\n\nWired into `PasteDiagramCommand::redo()`, right before the existing `newUuid()` loop, gated by the same `first_redo` flag so undo/redo cycles don't re-trigger either.\n\n## Also fixed: the same bug on the folio-duplication path\n\nTracing this turned up the identical gap in `ElementsPanelWidget::duplicateDiagram()`: same XML round-trip, same per-element `newUuid()` renewal loop (added by an earlier commit to fix the separate uuid-collision issue), but nothing ever resolving `tmp_uuids_link` there either. Same root cause, same fix, reusing the new overload — flagging this clearly since it's beyond discussion #607's literal scope (cut/copy/paste), not silently bundled in.\n\n## Test plan\n\n- [x] Full project builds and links clean (`cmake --build`, default `BUILD_WITH_KF5=ON` config), zero new warnings\n- [x] Existing Catch2 test suite passes (general regression check; no existing coverage of linked-element paste specifically)\n- [ ] Manual: link a master element to a slave, select both, copy, paste — confirm the pasted copies are linked to each other (not to the originals, and not silently dropped); paste only the master (partner not copied) — confirm it comes in unlinked, same as today\n- [ ] Manual: duplicate a folio containing a linked master/slave pair — confirm the duplicated copies are linked to each other","commits":[{"authoredDate":"2026-08-04T05:17:37Z","authors":[{"email":"shane.ringrose@gmail.com","id":"MDQ6VXNlcjEyMTE2MTEw","login":"ispyisail","name":"ispyisail"}],"committedDate":"2026-08-04T05:17:37Z","messageBody":"Copying an element writes its linked partners' uuids into the\nclipboard XML (Element::toXml), and Element::fromXml reads them back\ninto a deferred tmp_uuids_link buffer -- but only Element::initLink(),\ncalled only from Diagram::refreshContents() (itself only called on\nfull project load and macro-block insertion), ever resolves that\nbuffer into a real link. DiagramView::paste() calls\nDiagram::fromXml() directly and never calls refreshContents() or\ninitLink(), so pasted elements' pending links were silently dropped,\nwhether one or both halves of a linked pair were pasted together.\n\nAdd Element::initLink(const QList<Element *> &) -- resolves pending\nlinks against a caller-supplied candidate list instead of a\nproject-wide ElementProvider search. This scoping matters: right\nafter an XML round-trip and before PasteDiagramCommand::redo()'s\nexisting newUuid() loop renews each pasted element's identity, a\npasted element's tmp_uuids_link still holds its source's original\npartner uuid -- which at that exact moment still equals the\nnot-yet-renewed uuid of that partner's own pasted copy, if it was\npasted too. Resolving only within the pasted batch, not the whole\nproject, is what stops a linked pair being pasted together from\nmatching against an original element left elsewhere in the project\nthat happens to still carry that same soon-to-be-replaced uuid. If\nonly one half of a linked group is pasted, its entry finds no match\nin the batch and is dropped, same as today.\n\nAlso wire this into ElementsPanelWidget::duplicateDiagram(): tracing\nthe bug turned up the identical gap on that path (already fixed for\nthe separate uuid-collision issue in an earlier commit, but never for\nlink resolution) -- same XML round-trip, same newUuid() renewal loop,\nsame missing initLink() call, same fix.","messageHeadline":"Preserve master/slave links when pasting or duplicating a folio","oid":"aadfc4df6592d482084386bb36be6b8f23ecf1ad"},{"authoredDate":"2026-08-08T10:03:40Z","authors":[{"email":"shane.ringrose@gmail.com","id":"MDQ6VXNlcjEyMTE2MTEw","login":"ispyisail","name":"ispyisail"},{"email":"noreply@anthropic.com","id":"MDQ6VXNlcjgxODQ3","login":"claude","name":"Claude Opus 5"}],"committedDate":"2026-08-08T10:03:40Z","messageBody":"Following scorpio810's pointer to m_save_label_paste\n(\"Ne pas conserver les labels des éléments lors des copier coller\",\ndiagramcommands/erase-label-on-copy).\n\nThat preference is what makes preserving master/slave links safe on the paste\npath: with it at its default the pasted copies come in with no label, formula,\ncomment or location, so there is no duplicate designation to be ambiguous\nabout. ElementsPanelWidget::duplicateDiagram() never consulted it, so a\nduplicated folio always kept the source designations -- and once this branch\npreserves the links too, that produces two identically designated linked\ngroups, which is a genuinely ambiguous drawing rather than just untidy.\n\nThis is the case ChuckNr11 raised. His stated mechanism does not apply -- a\nduplicated link is not possible, SlaveElement::linkToElement() calls\nunlinkAllElements() first so a contact can only ever hold one master, and the\ncopies link to each other rather than to the originals -- but the underlying\nworry was right, and it was about labels, not links.\n\nDuplication now clears the same four fields paste clears, and conductor texts\nwith them, under the same preference. Users who deliberately turn the\npreference off keep the old behaviour on both paths, which is the point of\nhaving it.\n\nVerified: builds clean, CMake/Ninja Release, Qt 5.15, 813/813.\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>","messageHeadline":"Honour erase-label-on-copy when duplicating a folio","oid":"abb94386555d9c5c3593cfefdee3a0d9ecd87946"}],"number":659,"state":"OPEN","title":"Preserve master/slave links when pasting or duplicating a folio","url":"https://github.com/qelectrotech/qelectrotech-source-mirror/pull/659"}
