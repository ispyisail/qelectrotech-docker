{"body":"### Problem\n\n`QETProject::readProjectXml()` raises two blocking `QMessageBox` prompts while parsing a project: one when it was saved by a newer QElectroTech than the running build, and one when it was saved by 0.6 or older. Both ask the user whether to open anyway.\n\nIn a headless CLI run there is nobody to answer, so the modal never returns and the process hangs until it is killed. Because the prompt fires during *load*, before any export work starts, **every** CLI mode is affected.\n\nThe shipped example `examples/schema_indus.qet` (`version=\"0.3\"`) reproduces it. All eight of these hang indefinitely on current master:\n\n```\n--info  --export-bom  --export-nets  --export-links\n--export-cables  --export-wires  --resave  --export-pdf\n```\n\nI found this by running every example project through every CLI mode with a 120s timeout — that one project accounted for 8 timeouts, the only hangs in 185 runs.\n\n### Change\n\nAdd `QETProject::setInteractive()`, mirroring the existing `setBackupEnabled()` pattern, and disable interactivity in `main.cpp`'s CLI branch right next to the `setBackupEnabled(false)` call already there for the same class of reason.\n\nWith interactivity off, both cases report the version mismatch on stderr via `qWarning()` and continue as if the user had chosen Open, so the export still happens and the reason is visible in the log:\n\n```\nWarning: '.../examples/schema_indus.qet' was saved with QElectroTech 0.3 and is only\npartially compatible with this build (0.100.1); opening it anyway.\n```\n\nThe GUI path is untouched — both prompts still appear when a project is opened interactively.\n\n### Testing\n\nOn `examples/schema_indus.qet`, all eight modes went from hitting the 120s timeout to completing in under a second, with valid output (`--info` reports 48 elements / 69 conductors, `--export-pdf` writes a 21 KB PDF).\n\nRegression check: opened the same project in the GUI and confirmed the \"Avertissement\" dialog still appears as before.","commits":[{"authoredDate":"2026-08-13T07:59:10Z","authors":[{"email":"shane.ringrose@gmail.com","id":"MDQ6VXNlcjEyMTE2MTEw","login":"ispyisail","name":"ispyisail"}],"committedDate":"2026-08-13T07:59:10Z","messageBody":"QETProject::readProjectXml() raises two blocking QMessageBox prompts\nwhile parsing: one when the project was saved by a newer QElectroTech\nthan the running build, and one when it was saved by 0.6 or older. Both\nask the user whether to open anyway.\n\nIn a headless CLI run there is nobody to answer, so the modal never\nreturns and the process hangs until it is killed. Every CLI mode is\naffected, because the prompt fires during project load, before any\nexport work starts. The shipped example examples/schema_indus.qet\n(version=\"0.3\") reproduces it: --info, --export-bom, --export-nets,\n--export-links, --export-cables, --export-wires, --resave and\n--export-pdf all hang indefinitely.\n\nAdd QETProject::setInteractive(), mirroring the existing\nsetBackupEnabled() pattern, and have main.cpp disable interactivity in\nthe CLI branch alongside the backup call already there. When\ninteractivity is off, both cases report the version mismatch on stderr\nvia qWarning() and continue as if the user had chosen Open, so the\nexport still happens and the reason is visible in the log.\n\nThe GUI path is untouched: both prompts still appear when a project is\nopened interactively.\n\nVerified against schema_indus.qet: all eight CLI modes previously hit\nthe 120s test timeout and now complete in under a second with valid\noutput, and the prompt still appears when the same project is opened\nin the GUI.","messageHeadline":"Fix headless CLI hanging forever on version-incompatible projects","oid":"3fcc88c43d0a7836cad15277d95d0c4d88899149"}],"number":737,"state":"CLOSED","title":"Fix headless CLI hanging forever on version-incompatible projects","url":"https://github.com/qelectrotech/qelectrotech-source-mirror/pull/737"}
