# Docker build leftovers
valgrind-logs/
fuzzer-logs/
fuzzer-asan-logs/
fuzzer-tsan-logs/
asan-logs/
tsan-logs/
fuzz-logs/
edz-fuzz-logs/

# Screenshots and build output from the ad-hoc alignment investigations
# (test_alignment.sh regenerates align_test_out/)
align_test_out/
test_alignment_fix/build/

# Fetched from qet-elements-10-electric -- re-run the service to regenerate
elements-10-electric/

# ASan regression test artifacts (generated by tests/asan-regression/run.sh)
tests/asan-regression/raw/
tests/asan-regression/report.md

# Python
__pycache__/
*.pyc
*.pyo

# Editor files
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Simulator: raw sweep dumps are large and regeneratable (rerun
# `python3 -m simulator sweep`); simulator/reports/findings/ is curated
# and tracked deliberately.
simulator/reports/*.jsonl
scripts/helpers/wmstate
scenario-debug/

# regenerable gh API cache (rebuild with pr_evidence.py --refresh)
tools/pr-evidence/cache/

# bugtracker page cache (rebuild with build_corpus.py --refresh)
tools/bugtracker/cache/

# exportleak sweep: intermediate per-folio SVG exports (regenerated each run)
reports/exportleak-sweep-scratch/
# compiled LD_PRELOAD palette shim (build from tools/exportleak/paletteset.cpp)
tools/exportleak/*.so
